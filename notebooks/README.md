# Notebooks

Jupyter notebooks for exploration, prototyping, and analysis.

To use notebooks, install the optional notebook dependencies:

```bash
pip install -e .[dev,notebook]
```

Notebooks are for interactive work. Move reusable logic into `src/` and keep notebooks focused on exploration and presentation.
